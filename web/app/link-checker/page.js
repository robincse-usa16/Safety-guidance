import ToolPage from '@/components/ToolPage';
export const metadata = { title: 'Free Phishing Link Checker', description: 'Check whether a suspicious URL contains phishing, impersonation or unsafe link signals before opening it.', alternates: { canonical: '/link-checker' } };
export default function Page() { return <ToolPage eyebrow="PHISHING LINK CHECKER" title="Check if a link looks suspicious—before you click." description="Paste a URL safely. SafePay inspects the domain and address for common phishing, impersonation and hidden-destination signals." type="url" checks={['Look-alike and encoded domains','URL shorteners and hidden destinations','Raw IP and insecure HTTP links','Suspicious domain patterns']} steps={['Copy the link without opening it','Paste it into the checker','Review the domain evidence']} faq={[{q:'Does SafePay open the link in my browser?',a:'No. The current local engine analyzes the address without opening it in your browser.'},{q:'Does HTTPS prove a site is legitimate?',a:'No. HTTPS encrypts the connection but does not prove the business is trustworthy.'}]} />; }
