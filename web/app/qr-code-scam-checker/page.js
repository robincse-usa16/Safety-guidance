import ToolPage from '@/components/ToolPage';
export const metadata={title:'QR Code Scam Checker',description:'Decode a QR-code image locally and inspect its link or payment content before opening it.',alternates:{canonical:'/qr-code-scam-checker'}};
export default function Page(){return <ToolPage eyebrow="QR CODE SCAM CHECKER" title="See what a QR code contains before opening it." description="Upload a QR-code image. SafePay decodes it locally and sends the extracted link or text through the relevant risk check." type="qr" checks={['Local QR decoding','Embedded URL inspection','Payment and credential-request signals','Live threat lookup when configured']} steps={['Upload the QR image','Review the decoded destination','Analyze before opening or paying']} faq={[{q:'Does SafePay automatically open the QR destination?',a:'No. It decodes the content without navigating your browser to the destination.'},{q:'Can a QR code point to a safe-looking phishing page?',a:'Yes. Treat decoded links like any other untrusted URL and verify the destination independently.'}]}/>}
