import ToolPage from '@/components/ToolPage';
export const metadata={title:'Screenshot Scam Checker',description:'Upload a screenshot and extract suspicious message text locally before checking scam warning signs.',alternates:{canonical:'/screenshot-scam-checker'}};
export default function Page(){return <ToolPage eyebrow="SCREENSHOT SCAM CHECKER" title="Check a suspicious screenshot without retyping it." description="Upload a PNG, JPG or WebP image. SafePay extracts readable English text in your browser, lets you review it, and then analyzes the warning signs." type="screenshot" checks={['Local browser OCR','Message and email screenshots','Embedded suspicious link extraction','Editable text before analysis']} steps={['Upload a clear screenshot','Review the locally extracted text','Analyze the evidence and next steps']} faq={[{q:'Is the screenshot uploaded to the SafePay API?',a:'OCR runs in your browser. Only the extracted text you approve is sent for risk analysis.'},{q:'Why can OCR make mistakes?',a:'Blurry, cropped or stylized text can be misread. Always review extracted text before analysis.'}]}/>}
